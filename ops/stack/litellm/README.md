# ops/stack/litellm

Managed LiteLLM config surface.

Expected canonical file:
- `config.yaml`

Use:
- `updatecfg litellm --mode pull` to import runtime config
- `updatecfg litellm --mode push` to apply + restart service
