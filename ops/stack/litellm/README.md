# ops/stack/litellm

Managed LiteLLM config surface.

Expected canonical file:
- `config.yaml`

Use:
- `updatecfg litellm --pull` to import runtime config
- `updatecfg litellm --push` to apply + restart service
